type Nutritions = {
        [key : string] : number,
        amount: number,
        protein: number,
        fat: number,
        carbohydrates: number,
        calories: number
    }

export default Nutritions;